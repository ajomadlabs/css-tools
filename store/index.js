export const actions = {
  async nuxtServerInit({ dispatch }, context) {
    // const cookies = cookie.parse(context.req.headers.cookie || '')
    // console.log("Cookies Data in Nuxt Server Init", context.req.headers)
    // console.log('Nuxt Server Init Success')
  }
}

export default {
  actions
}